<h2>ジャンル検索結果</h2>

<table class="table table-hover table-inverse">
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td><%= post.title %></td>
          <td><%= post.content %></td>
        </tr>
      <% end %>
    </tbody>
</table>
