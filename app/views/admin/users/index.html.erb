<h1>会員一覧</h1>

<div>
  <% @users.each do |user| %>
   <li>
     <%= user.id %>　　<%= user.name %>　　<%= link_to "登録情報", admin_user_path(user) %>
     <%= link_to '削除', admin_user_path(user.id), method: :delete, data: { confirm: '本当に削除しますか？' } %>
   </li>
  <% end %>
</div>